# Variables de entorno ejemplo
# (copia a .env y ajusta antes de usar en producción)


# BACKEND
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000


# DB (no usado aún en Sprint0)
DATABASE_URL=postgresql://user:pass@localhost:5432/trading_db